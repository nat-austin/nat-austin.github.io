Place the edited discovery-set image in this folder and name it:

discovery-set.png

The homepage already references that exact path. Keep the original image file
unchanged so the browser preserves its natural colors and aspect ratio.
